import Foundation

/*class DIContainer {

    static let shared = DIContainer()
    private init() {}

    private let apiClient = APIClient.shared
    private let keychain  = KeychainStorage.shared

    lazy var authRepository:        AuthRepository        = AuthRepositoryImpl(apiClient: apiClient, keychain: keychain)
    lazy var annonceRepository:     AnnonceRepository     = AnnonceRepositoryImpl(apiClient: apiClient)
    lazy var messageRepository:     MessageRepository     = MessageRepositoryImpl(apiClient: apiClient)
    lazy var offreRepository:       OffreRepository       = OffreRepositoryImpl(apiClient: apiClient)
    lazy var transactionRepository: TransactionRepository = TransactionRepositoryImpl(apiClient: apiClient)
    lazy var livraisonRepository:   LivraisonRepository   = LivraisonRepositoryImpl(apiClient: apiClient)
    lazy var userRepository:        UserRepository        = UserRepositoryImpl(apiClient: apiClient)
    lazy var boostRepository:       BoostRepository       = BoostRepositoryImpl(apiClient: apiClient)
}*/
